{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm enrich && pnpm build",
  "outputDirectory": "out",
  "rewrites": [
    {
      "source": "/@:user",
      "destination": "/users/:user"
    },
    {
      "source": "/@:user/",
      "destination": "/users/:user/"
    }
  ]
}
